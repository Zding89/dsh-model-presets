/** Permanent bundle host entry; browser behavior is exported from ./client. */
export const name = 'dsh-model-presets'

/** Keep this package in the Loader tree so client-modules can discover it. */
export function apply() {}
